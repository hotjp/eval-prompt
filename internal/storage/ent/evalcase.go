// Code generated by ent, DO NOT EDIT.

package ent

import (
	"encoding/json"
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/eval-prompt/internal/storage/ent/evalcase"
	"github.com/eval-prompt/internal/storage/ent/schema"
)

// EvalCase is the model entity for the EvalCase schema.
type EvalCase struct {
	config `json:"-"`
	// ID of the ent.
	ID string `json:"id,omitempty"`
	// Name holds the value of the "name" field.
	Name string `json:"name,omitempty"`
	// Prompt holds the value of the "prompt" field.
	Prompt string `json:"prompt,omitempty"`
	// ShouldTrigger holds the value of the "should_trigger" field.
	ShouldTrigger bool `json:"should_trigger,omitempty"`
	// ExpectedOutput holds the value of the "expected_output" field.
	ExpectedOutput string `json:"expected_output,omitempty"`
	// Rubric holds the value of the "rubric" field.
	Rubric schema.Rubric `json:"rubric,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt    time.Time `json:"created_at,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*EvalCase) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case evalcase.FieldRubric:
			values[i] = new([]byte)
		case evalcase.FieldShouldTrigger:
			values[i] = new(sql.NullBool)
		case evalcase.FieldID, evalcase.FieldName, evalcase.FieldPrompt, evalcase.FieldExpectedOutput:
			values[i] = new(sql.NullString)
		case evalcase.FieldCreatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the EvalCase fields.
func (_m *EvalCase) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case evalcase.FieldID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value.Valid {
				_m.ID = value.String
			}
		case evalcase.FieldName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field name", values[i])
			} else if value.Valid {
				_m.Name = value.String
			}
		case evalcase.FieldPrompt:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field prompt", values[i])
			} else if value.Valid {
				_m.Prompt = value.String
			}
		case evalcase.FieldShouldTrigger:
			if value, ok := values[i].(*sql.NullBool); !ok {
				return fmt.Errorf("unexpected type %T for field should_trigger", values[i])
			} else if value.Valid {
				_m.ShouldTrigger = value.Bool
			}
		case evalcase.FieldExpectedOutput:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field expected_output", values[i])
			} else if value.Valid {
				_m.ExpectedOutput = value.String
			}
		case evalcase.FieldRubric:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field rubric", values[i])
			} else if value != nil && len(*value) > 0 {
				if err := json.Unmarshal(*value, &_m.Rubric); err != nil {
					return fmt.Errorf("unmarshal field rubric: %w", err)
				}
			}
		case evalcase.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the EvalCase.
// This includes values selected through modifiers, order, etc.
func (_m *EvalCase) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// Update returns a builder for updating this EvalCase.
// Note that you need to call EvalCase.Unwrap() before calling this method if this EvalCase
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *EvalCase) Update() *EvalCaseUpdateOne {
	return NewEvalCaseClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the EvalCase entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *EvalCase) Unwrap() *EvalCase {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: EvalCase is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *EvalCase) String() string {
	var builder strings.Builder
	builder.WriteString("EvalCase(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("name=")
	builder.WriteString(_m.Name)
	builder.WriteString(", ")
	builder.WriteString("prompt=")
	builder.WriteString(_m.Prompt)
	builder.WriteString(", ")
	builder.WriteString("should_trigger=")
	builder.WriteString(fmt.Sprintf("%v", _m.ShouldTrigger))
	builder.WriteString(", ")
	builder.WriteString("expected_output=")
	builder.WriteString(_m.ExpectedOutput)
	builder.WriteString(", ")
	builder.WriteString("rubric=")
	builder.WriteString(fmt.Sprintf("%v", _m.Rubric))
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// EvalCases is a parsable slice of EvalCase.
type EvalCases []*EvalCase
